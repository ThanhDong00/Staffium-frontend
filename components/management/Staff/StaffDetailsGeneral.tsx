import React from "react";

const StaffDetailsGeneral = ({ id }: { id: string }) => {
  return (
    <div className="bg-white p-5 rounded-lg shadow">
      <div>StaffDetailsGeneral + {id}</div>
    </div>
  );
};

export default StaffDetailsGeneral;
